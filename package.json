{
  "name": "delivery_recovery_mgr",
  "version": "1.0.0",
  "description": "This module is in charge of the data synchronizacion and compensation, it checks if data is not in the right place and puts it where it's supposed to be",
  "main": "sync_job.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jairo Guerra",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "firebase-admin": "^11.2.1",
    "mysql": "^2.18.1",
    "winston": "^3.8.2"
  }
}
